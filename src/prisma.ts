/**
 * Canonical generated Prisma namespace, values, models and input types.
 *
 * Consumers import generated contracts from this subpath and database access
 * from the package root. Keeping those entry points distinct prevents tests
 * that mock the database connection from accidentally replacing Decimal,
 * JsonNull and other generated Prisma values.
 */
export * from "./main-client/index.js";
